import asyncHandler from "../utils/asyncHandler";
import APIResponse from "../utils/APIResponse";
import { v2 as cloudinary } from "cloudinary";

const initialLoad = asyncHandler(async(req, res) => {
    
})

export {
    initialLoad
}